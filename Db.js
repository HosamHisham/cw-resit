const sqlite= require('sqlite3')
const db = new sqlite.Database('recipes.db')
const createUserTable= `CREATE TABLE IF NOT EXISTS USER (ID INTEGER PRIMARY KEY AUTOINCREMENT,
NAME TEXT NOT NULL,
EMAIL TEXT UNIQUE NOT NULL,
PASSWORD TEXT NOT NULL,
ISADMIN INT)`

const createRecipeTable=`CREATE TABLE IF NOT RECIPES FLIGHT (ID INTEGER PRIMARY KEY AUTOINCREMENT,
TITLE TEXT NOT NULL,
INGREDIENTS TEXT NOT NULL,
INSTURCTIONS TEXT NOT NULL,
QUANTITY INT NOT NULL)`

/*
const createBookingTable =`CREATE TABLE IF NOT EXISTS BOOKING (ID INTEGER PRIMARY KEY AUTOINCREMENT,
USER_ID INT,
FLIGHT_ID INT,
FOREIGN KEY (USER_ID) REFERENCES USER(ID),
FOREIGN KEY (FLIGHT_ID) REFERENCES FLIGHT(ID))`
*/

module.exports={db,createUserTable,createRecipeTable,}